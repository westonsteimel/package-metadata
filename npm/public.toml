name = "public"
ecosystem = "npm"
source = "https://github.com/tnantoka/public"
deprecated = true

# Strangely there is a module at https://www.npmjs.com/package/public.js but looking at the underlying reports, 
# they do indeed relate to https://www.npmjs.com/package/public, so not sure why this seemingly auto-generated cpe has
# the .js as neither the module or source repo have that.

[[cpe_configurations]]
vendor = "public.js_project"
product = "public.js"
target_software = "node.js"

[[cpe_configurations]]
vendor = "public_project"
product = "public"
target_software = "node.js"